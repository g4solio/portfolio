import { Chapters } from "davide-gozzi-portfolio";

// Self-contained: renders the full six-chapter work history (data ships in
// the bundle), including side/interlude/wide layouts and open decision notes.
export const WorkHistory = () => <Chapters />;
